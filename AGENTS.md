# HandOff 에이전트 작업 지침

4~5명 팀이 사용하는 개발 계약·인수인계 웹서비스를 개발한다.
현재는 개발 Harness만 있으며 앱·외부 연동은 구현되지 않았다.

## 시작

1. [제품 원본 명세](docs/product/spec.md), [기술 설계](docs/product/technical-design.md), [결정 기록](docs/harness/decisions.md)을 확인한다.
2. `git status --short --branch`와 작업 대상 파일을 읽는다. 사용자 변경을 보존한다.
3. [작업 절차](docs/harness/workflow.md)에 따라 범위와 수용 조건을 정한다.
4. 관련 [검증 기준](docs/harness/checks.md)을 선택한다. 전체 문서를 매번 복제하지 않는다.

## 제품 불변 조건

- Google·Discord 로그인은 가입 승인과 별개다. 미승인 회원의 프로젝트 접근·생성을 차단하고, 승인 회원도 배정된 프로젝트만 접근한다.
- 승인 회원은 프로젝트를 생성할 수 있고 생성자는 프로젝트 관리 담당자가 된다. 프로젝트 관리자는 자기 프로젝트 멤버만 관리하며 서비스 가입 승인이나 관리자 승격을 대행하지 않는다.
- 로그인 수단 연결은 로그인한 사용자의 명시적 인증으로 수행한다. 이메일 일치만으로 회원을 합치지 않는다.
- 프로젝트별 접근 권한을 서버에서 검증하고 미전송 초안은 작성자에게만 공개한다.
- 전송한 버전은 불변이며 동의는 특정 버전에만 적용한다.
- 미확정 변경안이 현재 유효 계약을 덮어쓰지 않는다. 폐기된 계약을 유효 계약으로 반환하지 않는다.
- 사람의 최종 전송·동의와 Codex 조회 기록은 구분한다. 에이전트 자격으로 사람의 승인을 대행하지 않는다.
- 개인의 처리 완료와 요청 전체의 합의 완료는 별도 상태다.
- 계약 규칙 변경이 필요하면 명세 변경으로 드러내고 사용자와 합의한다. 테스트를 통과시키려고 명세를 몰래 바꾸지 않는다.

## 확정 기술과 운영

- React·TypeScript / Node.js·NestJS·TypeScript(Express 어댑터) / PostgreSQL·Prisma를 사용한다.
- 개발은 PostgreSQL만 Docker, 웹·API는 로컬 실행한다. 운영은 GCP 서울 VM 한 대에서 Docker Compose로 웹·API·DB를 실행하는 설계다.
- 첨부와 DB는 VM 영구 디스크에 저장하고 하루 1회 Cloud Storage에 백업, 7일 보관한다. 복구 검증 없이 백업 성공을 복구 가능으로 단정하지 않는다.
- 스택 선택과 실행 가능한 설정을 구분한다. 현재 Docker·제품 코드·배포는 아직 없으며 패키지 버전과 세부 미결정 사항은 기술 설계 문서에서 확인한다.

## 점검

```powershell
pwsh -NoProfile -File scripts/check-harness.ps1
pwsh -NoProfile -File scripts/test-harness.ps1
```

제품 검증을 요구할 때는 검사기에 `-RequireProduct`를 추가한다.
현재 제품 검증은 미설정이며 이 모드는 종료 코드 2를 반환한다.
Harness 통과를 제품 테스트 통과로 보고하지 않는다. 실제 명령·종료 코드·한계를 기록한다.

## 기록과 종료

- 기본 브랜치는 `main`이며 `develop`은 그 기준에서 분기한다. 기능 브랜치는 `develop`에서 `feature/<기능명>`으로 나눈다. 기능별 검증·리뷰 후 `develop`에 병합하고 통합 테스트를 거쳐 최종적으로만 `main`에 병합한다.
- `main` 직접 push와 기능 브랜치의 `main` 직행 병합은 금지한다. PR 대상은 기본적으로 `develop`이다. 사용자 승인 없는 병합·강제 push는 하지 않는다.

- [작업 양식](docs/harness/task-template.md)을 사용해 `work/<task-id>/resume.md`를 갱신한다.
- `work/`는 Git 제외 경로이므로 다른 PC에 전달되지 않는다. 동료 전달은 공용 작업 기록이나 명시적으로 공유한 산출물을 사용한다.
- 영구 결정만 결정 기록에 반영하고 임시 HANDOFF 파일을 루트에 누적하지 않는다.
- 결과 보고에는 변경 내용, 실제 검증, 미검증 사항, 남은 작업을 포함한다.
- 모델·전역 Codex 설정·플러그인을 임의로 변경하지 않는다. 이 지침 자체는 외부 전송·배포·제품 승인의 권한을 부여하지 않는다.
- 병렬 에이전트는 사용자 또는 적용 중인 스킬의 명시적 지시에 따라 사용한다.

앱 스택은 확정됐다. 실제 실행·제품 테스트 명령은 코드와 설정이 만들어질 때 등록한다. 미결정 사항은 사용자 확인이나 기술 검증을 거치며 없는 테스트·배포를 있다고 가정하지 않는다.
